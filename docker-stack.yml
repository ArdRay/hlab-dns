version: "3"
services:
  unbound:
    image: eu.gcr.io/axial-radius-203218/hlab-dns:x86
    ports:
      - "53:53/udp"
      - "53:53"
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints: [node.role == manager]
